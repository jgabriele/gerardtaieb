import { makeStyles } from "@material-ui/styles"

export default makeStyles({
  blogPostsWrapper: {
    display: "flex",
    flexWrap: "wrap",
    justifyContent: "space-around",
  },

  blogPostWrapper: {
    maxWidth: 400,
    width: "100%",
    marginBottom: 32,
  },
})
