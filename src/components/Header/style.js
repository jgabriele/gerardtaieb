import { makeStyles } from "@material-ui/styles"

export default makeStyles({
  header: {
    color: "white",
    height: 60,
    display: "flex",
    padding: "0px 16px",
    zIndex: 1000,
    position: "relative",
    background: "#0e1e2b",
    alignItems: "center",
    justifyContent: "space-between",
  },
})
