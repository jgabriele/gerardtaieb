import { makeStyles } from "@material-ui/styles"

export default makeStyles({
  header: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    height: 60,
    color: "white",
    padding: "0px 16px",
    zIndex: 1000,
    position: "relative",
    background: "#0e1e2b",
  },

  logo: {
    height: 50,
  },
})
