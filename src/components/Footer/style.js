import { makeStyles } from "@material-ui/styles"

export default makeStyles({
  footer: {
    display: "flex",
    alignItems: "center",
    height: 80,
    color: "white",
    background: "#0e1e2b",
    padding: "0 32px",
    fontSize: 16,
  },
})
